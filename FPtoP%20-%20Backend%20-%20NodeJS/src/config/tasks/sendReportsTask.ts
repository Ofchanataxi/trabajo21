// src/tasks/sendReportsTask.ts
export async function sendReportsTask() {
  console.log('📩 Enviando reportes...');
  try {
    // Simular el envío de reportes
    console.log('✅ Reportes enviados correctamente.');
  } catch (error) {
    console.error('❌ Error enviando reportes:', error);
  }
}
