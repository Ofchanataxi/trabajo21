import { DateCustomFormatFromAvocetPipe } from './date-custom-format-from-avocet.pipe';

describe('DateCustomFormatFromAvocetPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCustomFormatFromAvocetPipe();
    expect(pipe).toBeTruthy();
  });
});
