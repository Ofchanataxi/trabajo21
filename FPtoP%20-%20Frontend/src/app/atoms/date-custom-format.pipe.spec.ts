import { DateCustomFormatPipe } from './date-custom-format.pipe';

describe('DateCustomFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCustomFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
