import { CustomShayaHeaderComponent } from './custom-shaya-header.component'

describe('CustomShayaHeaderComponent', () => {
  it('should mount', () => {
    cy.mount(CustomShayaHeaderComponent)
  })
})