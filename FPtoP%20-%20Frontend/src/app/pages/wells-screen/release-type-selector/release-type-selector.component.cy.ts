import { ReleaseTypeSelectorComponent } from './release-type-selector.component'

describe('ReleaseTypeSelectorComponent', () => {
  it('should mount', () => {
    cy.mount(ReleaseTypeSelectorComponent)
  })
})